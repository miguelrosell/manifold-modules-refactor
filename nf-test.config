config {
    // Directory where tests are located
    testsDir "modules/local"
    
    // Temporary work directory for tests
    workDir ".nf-test"
    
    // Your pipeline config file
    configFile "nextflow.config"
    
    // Force docker usage
    profile "docker"
}
